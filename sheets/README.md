Drop the operator's weekly xlsx here. The newest one is used automatically.

The files themselves are gitignored: they are the league's, not the repo's.

If Google Drive for desktop is installed, point `sheets.directory` in params.yaml
at `OW Pick Em/26-27` instead and this folder becomes unnecessary.
