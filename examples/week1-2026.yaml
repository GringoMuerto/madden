# Week file: what the missing schedule feed would otherwise supply.
# Temperature and wind come from open-meteo (madden/weather.py) at each game's kickoff.
# Anything entered here OVERRIDES the forecast, so enter a value only when you have a
# source on the spec's data-layer list. Never from a sports or betting site.
week: 1
season: 2026

neutral_sites:
  - "SF@LAR"          # Melbourne Cricket Ground. The operator capitalised the Rams;
                      # they are not at home. Home rules void.

temperatures: {}      # forecast supplies these; override only from an allowlisted source
wind_mph: {}          # forecast supplies these; override only from an allowlisted source

retractable_open: []  # none reported open
blind: []             # no Call A yet, so structural breaks are declared by hand
